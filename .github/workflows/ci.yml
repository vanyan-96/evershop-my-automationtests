name: CI Pipeline

on:
  push:
    branches:
      - main  # or your preferred branch
  pull_request:
    branches:
      - main  # or your preferred branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Install Docker Compose
        run: |
          sudo apt-get update
          sudo apt-get install -y docker-compose

      - name: Build and Run Docker Compose
        run: |
          docker-compose up -d
          # Add your commands to run tests here, for example:
          docker exec evershop-my-automationtests-app-1 npm run user:create -- --email "rosievanyan@gmail.com" --password "Leovinchi6" --name "testik"
